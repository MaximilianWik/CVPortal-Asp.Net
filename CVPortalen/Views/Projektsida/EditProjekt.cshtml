@model Projekt
@* Vyn för att redigera sitt projekt
 *@<h2>Edit Project</h2>

<form asp-action="EditProjekt" asp-controller="Projektsida" method="post">
    <input type="hidden" asp-for="ProjektId" />

    <div class="form-group">
        <label asp-for="ProjektName"></label>
        <input asp-for="ProjektName" class="form-control" />
        <span asp-validation-for="ProjektName" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Artal"></label>
        <input asp-for="Artal" class="form-control" />
        <span asp-validation-for="Artal" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Infromation"></label>
        <textarea asp-for="Infromation" class="form-control"></textarea>
        <span asp-validation-for="Infromation" class="text-danger"></span>
    </div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary">Save Changes</button>
        <a asp-action="ProjektDetails" asp-controller="Projektsida" asp-route-id="@Model.ProjektId" class="btn btn-secondary">Cancel</a>
    </div>
</form>


@section scripts {
    <script>
        // function saveChanges() {
        //     var formData = {
        //         ProjektId: $('#ProjektId').val(),
        //         ProjektName: $('#ProjektName').val(),
        //         Artal: $('#Artal').val(),
        //         Infromation: $('#Infromation').val()
        //     };

        //     $.ajax({
        //         url: '@Url.Action("EditProjekt", "Projektsida")',
        //         type: 'POST',
        //         contentType: 'application/json',
        //         data: JSON.stringify(formData),
        //         success: function (result) {
        //             alert(result.message);

        //             if (result.success) {
        //                 location.reload();
        //             } else {
        //                 if (result.errors && result.errors.length > 0) {
        //                     result.errors.forEach(function (error) {
        //                         console.error("Validation Error:", error);
        //                     });
        //                 } else {
        //                     console.error("Unknown Validation Error");
        //                 }
        //             }
        //         },
        //         error: function (error) {
        //             console.error("Error:", error);
        //         }
        //     });
        // }
    </script>
}






